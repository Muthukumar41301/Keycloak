#DataSource Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/Keycloak
spring.datasource.username=postgres
spring.datasource.password=Password@123

spring.liquibase.change-log=classpath:/db/changelog/db.changelog-master.yaml

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Keycloak Configuration
keycloak.auth-server-url=http://localhost:8080/auth
keycloak.realm=myrealm
keycloak.resource=mk-department
keycloak.credentials.secret=bQx7OmFh0NsMlTQGjoymF0vLFfkF6WBY
keycloak.bearer-only=true

# Enable JTA transactions if needed
spring.jta.transaction-manager-type=atomikos